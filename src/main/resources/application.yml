spring:
  application:
    name: prestamos
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

---
# Perfil dev — PostgreSQL local (lo afinará Andrea en la Fase 2).
# Credenciales por variable de entorno; nunca hardcodeadas en el repo.
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/prestamos}
    username: ${DB_USER:prestamos}
    password: ${DB_PASSWORD:prestamos}
  # Flyway es la fuente de verdad del esquema; Hibernate solo valida que las
  # entidades coincidan con el esquema migrado (nunca genera ni altera tablas).
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
